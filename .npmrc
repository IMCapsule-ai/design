# Use hoisted (npm-style flat) node_modules layout so electron-builder's
# production-deps walk doesn't get stuck traversing pnpm symlinks. Critical
# for Windows runners where symlink resolution is slow enough to make the
# release build hang for 30+ minutes.
node-linker=hoisted
